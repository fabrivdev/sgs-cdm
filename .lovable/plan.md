1. **Colores de marca** en `DistribucionMarca` (`src/pages/Dashboard.tsx`): CLAAS verde `#00853E`, HORSCH rojo `#E2001A`, OTROS gris neutro. Cada barra usa shades del color de la marca: claro = abiertos, medio = pausados, oscuro = cerrados. Leyendas debajo ajustadas.

2. **Horas reales del período** en `cargaMarca`: en vez de sumar `r.horas` (histórico del trabajo), sumar sólo las jornadas `Completado` cuya `fecha` cae en `[periodStart, periodEnd]`. Esto se hace recorriendo `jornadaFechas`/jornadas dentro del trabajo. Para no recargar el cálculo, agrego `horasPeriodo` a cada fila de `trabajosBase` (horas Completado con fecha en el período) y `cargaMarca` lo suma.